<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<!--  the code template created by wxmp-generator @author hermit @version 2.0 @date 2018-04-17 10:43:21 -->
<mapper namespace="com.wxmp.wxcms.mapper.TbEmpDao">
    <!--根据用户名密码查询-->
    <select id="getSysUser" parameterType="com.wxmp.wxcms.domain.TbEmp"
    	resultType="com.wxmp.wxcms.domain.TbEmp">
		SELECT * FROM tb_emp t
		where t.username =  #{userName}
		and t.pwd = #{pwd}
	</select>
	
	<!--根据主键id查询用户-->
    <select id="getSysUserById" parameterType="java.lang.String"
    	resultType="com.wxmp.wxcms.domain.TbEmp">
		SELECT * FROM tb_emp t
		where t.id =  #{id}
	</select>
	
	
    <!--修改登录密码-->
	<update id="updateLoginPwd" parameterType="com.wxmp.wxcms.domain.TbEmp" >
		UPDATE tb_emp
		SET pwd = #{newpwd},
		    update_time = #{updateTime} 
		WHERE id = #{id} 
	</update>
</mapper>